- title I18n.t('views.categories.index.page_title')
.container
  %h1= I18n.t('views.categories.index.title')
  %ul.nav.nav-tabs.nav-stacked
    - @categories.each do |category|
      %li
        = link_to (default_locale? ? category_path(category.title) : locale_category_path(I18n.locale,category.title)), title: "Category: #{category.title}" do
          %table{width: '100%'}
            %tbody
              %tr
                %td{width: '10%'}
                  - if user_signed_in?
                    - if category.users.include? current_user
                      %span.unsubscribe{id: category.id, title: 'unsubscribe'}
                    - else
                      %span.subscribe{id: category.id, title: 'subscribe'}
                %td{width: '20%'}
                  = "#{category.title_locale(I18n.locale).blank? ? category.title : category.title_locale(I18n.locale)} (#{category.pictures_count})"
                %td.sub{align: 'center'}
                  -category.included_pictures.each do |pic|
                    = image_tag pic.image.url(:default), alt: pic.image_file_name
                %td{width: 128}
                  - if category.users.include? current_user
                    = image_tag 'subscribe.png', width: 128
